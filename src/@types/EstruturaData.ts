export interface EstruturaData {
    tituloEstrutura: string;
    textoEstrutura: string;
    estrutura: string;
    imagemFachada: {
        node: {
            mediaItemUrl: string
        }
    }
}